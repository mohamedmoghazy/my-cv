// scraper removed
// The original scraper script (which used Puppeteer to fetch Wix pages and generate
// src/Components/ProjectsData.jsx) has been intentionally removed.
//
// If you need to re-run scraping in the future, reintroduce a script that
// regenerates `src/Components/ProjectsData.jsx` or keep a copy in version control.
// For now this file is a no-op placeholder to avoid accidental execution.

console.log('scripts/scrape-wix.js: removed.');
